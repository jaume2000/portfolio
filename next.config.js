/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ["default","en","es","fr"],
        defaultLocale: "deafult",
        localeDetection: true
    }
}

module.exports = nextConfig
